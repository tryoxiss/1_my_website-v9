<ul data-component class="ratio-gallery gallery-flex">
    <slot />
</ul>
  
<style is:inline>
p { 
    margin: 0;
}

/***
 🟣 SmolCSS Snippet Styles
 */
.gallery-flex {
    --min: 15ch;
    --_gap: 1rem;

    display: flex;
    flex-wrap: wrap;
    gap: var(--_gap);
}

.gallery-flex > * {
    flex: 1 1 var(--min);
}

.ratio-gallery {
    --min: 15rem;
    --aspect-ratio: 4/3;
    --gap: 100;

    margin-left: -2.5rem;

    gap: 0.5rem;
}

.ratio-gallery li {
    height: max(25vh, 15rem) + 2rem;
    list-style: none;
}

.ratio-gallery li::marker { 
    content: "";
}

.with-credits li { 
    margin-bottom: 1.75rem;
}

@supports (aspect-ratio: 1) {
    .ratio-gallery li {
        aspect-ratio: var(--aspect-ratio);
        height: auto;
    }
}

.ratio-gallery img {
    display: block;
    object-fit: cover;
    width: 100%;
    height: 100%;

    border-radius: var(--border-radius);
}
</style>